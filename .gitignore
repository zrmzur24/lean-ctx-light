# Dependencies
node_modules/

# Build artifacts (this project doesn't emit, but just in case)
dist/
build/
*.tsbuildinfo

# Test coverage
coverage/
.nyc_output/

# Editor/IDE
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS-specific
# Windows
desktop.ini

# lean-ctx runtime artifacts (if doctor.sh is run inside the repo by mistake)
.lean-ctx/

# Secrets (shouldn't exist here but belt-and-suspenders)
.env
.env.local
.env.*.local
